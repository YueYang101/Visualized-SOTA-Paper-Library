# 两模型工作流 Prompt

把这段作为主模型处理“代读并加入文献地图”任务时的长期规则。

## 可复制 Prompt

每次有一篇新论文需要代读或加入文献地图，必须把工作拆给两个模型：

1. **论文预读模型**：为这一篇论文新建一个独立 Agent，使用新鲜、最小化的上下文。只向它提供论文 PDF/URL/阅读笔记、当前研究关注点，以及 `LLM_PRE_READ_PROMPT.md` 的完整内容。它只做论文阅读、元数据核验、分类建议和结构化知识卡，禁止修改网站、运行网站构建或同步交付目录。
2. **网站维护模型**：由当前主模型担任。它不重新读取整篇论文，只接收预读 Agent 的 JSON、250 字以内交接摘要和必要证据；负责查重、检查字段一致性、解决不确定项、写入分类索引与独立详情分片、更新计数、构建检查和同步网站。

上下文规则：

- 每篇新论文都新建一个预读 Agent；多篇论文可一篇一个 Agent 并行处理。
- 新建预读 Agent 时优先使用不继承历史对话的模式；不要把整个网站源码或无关对话传给它。
- 预读 Agent 不得再派生负责网站修改的 Agent；网站只有主模型可以编辑，避免并发覆盖。
- 附件已经包含代读笔记时，预读 Agent 仍需做轻量审计、规范化和元数据核验，但不必重复全文逐页解读，除非笔记存在明显证据缺口。
- 主模型发现分类、链接、精读状态或关键数字矛盾时，把具体问题退回同一个预读 Agent 复核；不要在主上下文重新装入全文。
- 预读 Agent 的结构化结果是候选数据，不是自动可信事实；主模型仍需做查重、schema 校验和网站构建检查。
- 如果当前环境无法新建独立 Agent，先明确告诉用户，不能悄悄把两个角色合并到同一上下文。

执行顺序：`新建预读 Agent → 接收结构化知识卡 → 主模型查重/核验 → 需要时向预读 Agent 追问 → 主模型录入网站 → 检查和同步`。
